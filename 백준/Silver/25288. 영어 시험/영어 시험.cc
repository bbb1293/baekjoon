#include <bits/stdc++.h>

using namespace std;

int n;
char s[27];

int main() {
    // freopen("input.txt", "r", stdin);

    scanf("%d", &n);
    scanf("%s", s);

    while(n--) {
        printf("%s",s);
    }

    return 0;
}
