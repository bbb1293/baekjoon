#include <bits/stdc++.h>

using namespace std;

int n;

int main() {
    // freopen("input.txt", "r", stdin);

    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> n;

    int k;
    cin >> k;

    cout << k << "\n";
    while (k--) {
        string tmp;
        cin >> tmp;

        cout << tmp << "\n";
    }

    return 0;
}