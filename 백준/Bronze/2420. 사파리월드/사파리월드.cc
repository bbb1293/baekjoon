#include <bits/stdc++.h>

using namespace std;

int main() {
    // freopen("input.txt", "r", stdin);

    long long a, b;
    scanf("%lld %lld", &a, &b);

    printf("%lld", abs(a - b));

    return 0;
}
